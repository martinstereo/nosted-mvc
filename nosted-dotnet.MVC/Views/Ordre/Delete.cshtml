@model nosted_dotnet.MVC.Entities.Ordre

<h1>Delete Ordre</h1>

<!-- Include your custom JavaScript file -->
<script src="js/site.js" defer></script>

@using (Html.BeginForm("DeleteConfirmed", "Ordre", new { id = Model.Id }, FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <p>Er du sikker på at du vil slette denne ordren?</p>
    <input type="submit" value="Slett" class="btn btn-danger" />
    <a href="@Url.Action("Index", "Ordre")" class="btn btn-secondary">Avbryt</a>
}
